# Android PDA UI 重构协作规范

本文件用于指导 `warehouse-pda-android` 的 UI/交互重构。后续任何涉及界面、流程、视觉风格、组件抽象、扫码作业体验的修改，都必须优先遵守本文件。

## 项目定位

- 这是箱码流向追踪 PDA 作业端，不是库存管理后台。
- 主要运行设备是安卓 PDA 扫码枪，也可能在普通安卓手机上测试。
- 核心目标是快速记录每个唯一箱码的去向：少看屏幕、少选字段、连续扫码、少误操作。
- PDA 端允许保留必要查询能力，例如扫码查询；但不得迁入 Web 端的维护、统计、复杂筛选和后台管理能力。
- Web 端负责完整业务管理，PDA 端只负责作业执行和现场确认。

## 当前 UI 重构目标

本轮不是继续零散美化单个页面，而是重建一套统一 UI 系统：

- 建立统一视觉语言，避免每个页面像不同产品。
- 重建快速出库、扫码回库、查询、我的等页面的信息层级。
- 统一卡片、按钮、表单、扫码输入、状态反馈、底部操作区。
- 优先保证 PDA 现场可用性，再考虑视觉装饰。
- 所有新页面必须先有明确交互结构，再写代码。

## 设计方向

全局 UI 基准为 `/Users/suhengtan/Desktop/安卓ui/首页/`：

- `screen.png` 是首页视觉基准。
- `DESIGN.md` 中的 `Industrial Precision` 是当前阶段最高优先级的设计规范。

整体风格应接近该首页参考图：

- 浅灰紫背景 `#FAF8FF`。
- 顶部居中品牌标题 `仓库管理系统`。
- 大标题、强信息层级。
- 白色大圆角任务卡片承载主要入口。
- 高对比蓝色 `#0052CC` 作为主操作色。
- 出库作业使用深橙色 `#A33500` 强调。
- 橙色用于待处理、警示或次级强调。
- 绿色用于成功、已签收、完成状态。
- 红色只用于错误、异常、危险操作。
- 圆角、留白、阴影要统一，不要混用多套卡片样式。
- 底部导航使用白色浮动栏，当前 Tab 使用蓝色胶囊。

不得出现：

- 过度柔和、低对比、看不清的颜色。
- 一页内太多解释性文字。
- 重复标题，例如页面标题和卡片标题表达同一件事。
- 为了“好看”牺牲扫码效率。
- 表单、卡片、按钮尺寸忽大忽小。
- 商品名竖排、按钮挤压、横向溢出。

## PDA 交互原则

- 扫码枪输入优先，软键盘输入只是兜底。
- 扫码页必须保持输入焦点，扫码回车应自动加入。
- 成功和失败必须有明显反馈，优先使用声音 + 颜色 + 简短文字。
- 现场作业页面不写长说明，正式版只保留必要字段名和状态。
- 关键按钮要足够大，适合戴手套或单手操作。
- 页面底部固定主操作按钮，但不能遮挡核心内容。
- 异常条码必须可见、可定位、可删除。
- 添加、删除、提交等动作要避免误触，危险操作需弱化或二次确认。

## 信息架构

PDA 端保留底部导航：

- 首页
- 查询
- 回库
- 出库
- 我的

日常业务只有三项：

1. `快速出库`
   - 选择来源仓库。
   - 选择去向为销售人员或目标仓库。
   - 连续扫描混合箱码，单次最多 500 个。
   - 不选择商品，不填写目标数量，不校验数量库存。
2. `扫码回库`
   - 选择回库仓库。
   - 连续扫描未售货物、店铺退货或未知外部退货的混合箱码。
   - 不选择商品、销售人员、店铺，不填写生产日期。
3. `条码查询`
   - 精确查询箱码当前归属、待签收/已签收/签收异常状态。
   - 展示出库销售人员、回库记录、勤策收货单位和完整履历。

商品名称、单位和终端店铺来自勤策签收回填，不在 PDA 本地维护或手工选择。

## 页面设计流程

任何 UI 重构任务按以下顺序执行：

1. 先复述页面目标和层级，确认页面到底是哪一层。
2. 列出页面必须保留的信息和必须删除的信息。
3. 给出文字版 UI 方案，包括页面结构、卡片顺序、按钮位置、状态反馈。
4. 再改代码。
5. 编译安装到模拟器或真机。
6. 截图检查。
7. 发现不协调的地方继续改。

不要在没有确认页面层级时直接实现。尤其要区分“出库单页面”和“单个货品扫码页面”。

## 组件系统建议

后续应逐步把 `WarehousePdaApp.kt` 中的大量页面内联 UI 拆成可复用组件。

建议组件分层：

- `PdaScreenScaffold`
- `PdaTopBar`
- `PdaBottomActionBar`
- `PdaInfoCard`
- `PdaProgressCard`
- `PdaFormRow`
- `PdaSelectRow`
- `PdaGoodsLineCard`
- `PdaScanInputCard`
- `PdaScanRecordList`
- `PdaStatusChip`
- `PdaEmptyState`

设计 token 应集中管理：

- 颜色
- 字号
- 圆角
- 阴影
- 间距
- 按钮高度
- 卡片内边距

不要继续在页面里随手写新的颜色、圆角和间距。

## 视觉规则

推荐基础 token：

- 页面背景：`#FAF8FF`，不使用纯黑作为作业页面背景，除非用户明确要求。
- 主色：`#0052CC`。
- 标题深蓝：`#003D9B`。
- 正文：`#191B23`。
- 次级文字：`#434654`。
- 分隔线/边框：`#C3C6D6`。
- 出库强调：`#A33500`。
- 主按钮高度：至少 56dp。
- PDA 常用触控目标：至少 48dp。
- 卡片圆角：统一 24dp。
- 按钮和输入框圆角：8dp 到 12dp。
- 页面左右边距：20dp。
- 卡片间距：16dp。
- 标题字号要明显，但不要让首屏只剩标题和表单。

首页必须优先对齐参考图：

- 顶部居中 `仓库管理系统`。
- 问候语：`你好，{当前用户}`。
- 主标题：`请选择作业任务`。
- 右侧仓库胶囊选择器。
- 三张任务卡片：`条码查询`、`扫码回库`、`快速出库`。
- 底部导航：首页、查询、回库、出库、我的。

文字规则：

- 页面标题短而明确。
- 卡片标题只在需要区分信息块时使用。
- 操作提示尽量省略。
- 如果必须提示，控制在一行内。
- 不使用大段说明文。

## 快速出库 UI 重点

快速出库只需要两层：

1. 参数页：来源仓库、去向类型、销售人员或目标仓库。
2. 扫码页：已扫数量、剩余容量、扫码输入、校验状态清单和提交按钮。

不要显示商品选择、SKU 行、库存、目标数量或生产日期。来源仓库在扫码后锁定，清空条码后才允许更换。

## 业务规则不能被 UI 破坏

UI 重构不得改变以下业务规则，除非用户明确要求：

- 出库和回库只记录一次操作人员，默认当前登录账号。
- 快速出库允许不同商品的箱码混扫，不要求提前知道商品。
- 商品和终端店铺以勤策签收数据为准。
- 条码重复、归属异常和已核销必须即时提示。
- 出库扫码后锁定来源仓库；回库扫码后锁定回库仓库。
- 单次最多 500 个条码，全部明确校验通过后才能提交。
- 当前归属和签收状态是两个独立字段，不得互相替代。

## 验收标准

每次 UI 修改后至少完成：

- `./gradlew :app:assembleDebug`
- 安装到模拟器或真机。

## v0.2.0 可靠性基线

- 默认生产接口为 `http://43.108.14.102/`，登录前必须通过 `/api/health` 且 API 合约版本为 `1`。
- 快速出库和扫码回库必须携带可选的 `clientRequestId`；结果未知时复用原编号，不得生成新编号重复记账。
- 单次作业最多 500 个条码，所有条码明确校验通过后才允许提交。
- 快速出库扫码后锁定来源仓库；扫码回库后锁定回库仓库。
- 账号密码使用 Android Keystore 加密，禁止恢复明文 SharedPreferences 存储。
- Debug 与 Release 均不得输出 HTTP 请求体、Cookie、密码或条码。
- 正式版本使用 `~/.config/warehouse-pda/signing/` 下的长期签名材料，不得提交签名文件或密码。
- `v0.2.0` 允许从旧 Debug 版卸载重装；后续版本必须保持相同 applicationId 与正式签名以支持覆盖更新。
- 截图检查目标页面。
- 检查窄屏下是否横向溢出。
- 检查文字是否竖排或被截断。
- 检查底部按钮是否遮挡内容。
- 检查扫码输入焦点和回车加入行为。

如果是原生 Android 页面，不要声称使用 Playwright 完成验证。Playwright 适合 Web 页面；原生 App 应使用 Android Studio、模拟器、ADB、截图或真机测试。

## 沟通方式

- 如果用户说“这个页面不对”，先确认是页面层级不对、信息架构不对，还是视觉风格不对。
- 不要急着解释为什么当前实现合理。
- 先复述用户想要保留和删除的元素。
- 如果参考图和业务需求冲突，先指出冲突，再给两种方案。
- 不要过分夸赞设计，优先准确判断是否符合 PDA 现场使用。

## 禁止事项

- 不要把 Web 管理后台能力搬到 PDA。
- 不要为了快速实现，把所有页面继续塞进一个巨大的 Compose 文件。
- 不要在没有截图检查的情况下宣称 UI 完成。
- 不要忽略用户提供的参考图。
- 不要把“出库单页面”和“单个货品扫码页面”混为一谈。
- 不要使用无法解释来源的一次性视觉样式。
